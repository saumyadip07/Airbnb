<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 20px;
        }
        .container {
            max-width: 900px;
            margin: auto;
        }
        .profile-section {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 30px;
        }
        .alert-container {
            position: absolute;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: 100%;
            max-width: 500px;
            z-index: 9999;
        }
    </style>
</head>
<body>
    <div class="alert-container">
        <% if (success && success.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= success %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">X</button>
        </div>
        <% } %>
        <% if (error && error.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= error %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">X</button>
        </div>
        <% } %>
    </div>






    








    <div class="container mt-5">
        <button > <a href="/">Go Back to Home</a></button>
        <h1 class="mb-4">Welcome, <%= user.name %>!</h1>

        <% if (bookings.length > 0) { %>
            <h2>Your Bookings</h2>
            <div class="row">
                <% bookings.forEach(booking => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <img src="<%= booking.hotelImage %>" class="card-img-top" alt="<%= booking.name %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= booking.name %></h5>
                                <p class="card-text"><%= booking.description %></p>
                                <p class="card-text"><strong>Address:</strong> <%= booking.address %>, <%= booking.city %></p>
                                <p class="card-text"><strong>Price:</strong> ₹<%= booking.price %></p>
                                <p>Contact @ <%= booking.vendorEmail || 'N/A' %> for more details</p>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <h2>No bookings at the moment.</h2>
            <p>It looks like you haven't made any bookings yet. Explore our hotels and make your first booking!</p>
        <% } %>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
